mov 60 @fps

def START_OF_ROM #8000

mrk gameloop

    mov 0 @bg
    mov 0 x
    mov START_OF_ROM @rom

    mrk nextinput
        mov @rom t
        fjp noinput
        mov t @text
    jmp nextinput
    mrk noinput


    mov @minput t
    fjp nominput
        prn t
    mrk nominput

    nxt

jmp gameloop
