mov 60 @fps

def START_OF_ROM #8000

mrk gameloop

    mov 0 @bg
    mov #ffff @tcol
    mov 0 x
    mov 0 y
    mov START_OF_ROM @rom

    mrk nextinput
        mov @rom t
        fjp noinput
        mov t @dtxt
    jmp nextinput
    mrk noinput

    mov 0 x
    mov 10 y
    mov #8000 @dnum
    mov b00000_110000_00100 @tcol
    mov 'L' @dtxt
    mov 'o' @dtxt
    mov 'l' @dtxt

    mov @minput t
    fjp nominput
        prn t
    mrk nominput

    nxt

jmp gameloop
