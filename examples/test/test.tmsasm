mov 60 @fps

mrk gameloop

mov 0 @bg

mov 0 x

mrk nextinput
mov @input t
fjp noinput
    mov t @text
    jmp nextinput
mrk noinput


mov @minput t
fjp nominput
    prn t
mrk nominput

nxt

jmp gameloop
