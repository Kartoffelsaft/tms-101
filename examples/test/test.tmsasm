mov 60 @fps

mrk gameloop

    mov 0 @bg
    mov 0 x
    mov #8000 @rom

    mrk nextinput
        mov @rom t
        fjp noinput
        mov t @text
    jmp nextinput
    mrk noinput


    mov @minput t
    fjp nominput
        prn t
    mrk nominput

    nxt

jmp gameloop
